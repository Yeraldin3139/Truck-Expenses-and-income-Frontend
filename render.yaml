services:
  - type: static_site
    name: truck-frontend
    rootDir: .
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    autoDeploy: true
    envVars:
      - key: VITE_API_BASE_URL
        value: https://REEMPLAZA-CON-TU-BACKEND.onrender.com/api